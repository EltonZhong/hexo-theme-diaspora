<%
    posts = page.posts.slice(start, end);
    console.log(start, end)
    posts.sort("date", "desc").each(function(post, i){
        console.log(post.source);
        if (page.prev == 0 && i < start) {
            return true;
        }
%>
<%- partial('post/item', {post: post}) %>
<% }) %>